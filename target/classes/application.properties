spring.profiles.active=dev
spring.main.banner-mode=off

















































#spring.application.name=ticketbooking

# Server Config
#server.port=8080

# Database Connection Config
#spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ticket_booking_db}?useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=${DB_USER:root}
#spring.datasource.password=${DB_PASSWORD:}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Config
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true







































#spring.application.name=ticketbooking

# Server Configuration
#server.port=8080

# Database Connection Configuration (Cloud & Docker Friendly)
#spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ticket_booking_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=${DB_USER:root}
#spring.datasource.password=${DB_PASSWORD:ADMIN99}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Cloud Properties
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# H2 Console Security Note:
# Kyunki hum MySQL use kar rahe hain, aap SecurityConfig se /h2-console/** permitAll nikal sakte hain safety ke liye.